# Indonesian Dot Puzzle 

Demo:

![img](/puzzle.gif)

Uses: 
* React hooks
* Redux
* Context
* Styled Components

To run: 
* Run `npm install` to get the dependencies
* Run `npm start` to start the server